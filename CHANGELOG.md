# Changelog

本项目的重要变更记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，并尽量遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [v1.18.0] - 2026-08-14

### Added

- 数据库工作流优化：新增 `work_order_status_history` 状态历史表与 `trg_wo_status_history` 触发器（status/art_status 变更自动留痕，应用层零改动）；日志表与工单表新增查询索引；`work_orders.version` 乐观锁列（迁移脚本 `scripts/migrate_workflow_v1.sql`）。
- 办理工单（所有角色对话框）打开前从数据库重新查询最新数据（`dispatcher.py`），不再使用列表快照，按钮/门禁判断基于实时状态；工单详情双击打开同样实时刷新。
- 状态筛选下拉补充美工链状态（美工设计中/美工待分发/美工已完成）。

### Changed

- `status_sync` 全面重构：所有状态写入带乐观锁版本检查（并发修改提示刷新而非静默覆盖）；状态/art_status/时间字段合并为单条原子 UPDATE（消除半写窗口期）；`update_time_with_api` 支持 `status_new`/`art_status_new` 组合写入；新增 `update_local_status_only` 供美工链纯本地写入。
- 美工链（领取/选成品/分发）与美工后期审批只写 `art_status`，不再写全局 `status`，消除与剪辑链状态互相覆盖的问题。
- 视频审核 / 视频后期审核通过、退回：状态同步失败即中止（不再"假成功"发通知/关窗/报成功）；退回改为先同步状态、成功后再移动文件。
- "视频后期审核通过"日志仅在成功后写入，修复 `has_pending_edit_review` 门禁因失败日志永久失效导致的工单卡死。
- 视频后期审核门禁仅允许「视频后期审核中」状态（剪辑分发后的「后期已完成」不可再审，避免状态降级）。
- 摄影上传/分发：内存状态仅在同步成功后更新；通知仅在成功后发送；视频分发按视频扩展名校验（纯视频工单可分发）。
- 美工后期审批通过/退回：每次点击重新扫描待审批目录（修复部分文件移动失败后重试死循环）。
- 剪辑对话框：新增「后期已完成」按钮分支（可补发另一通道）；「视频后期审核中」时禁止重复提交审核；分发/领取增加路径存在性与非空校验（拦截空任务假成功推进状态）。
- 运营/销售领取素材：空目录与目录读取失败拦截；状态同步失败不再报"领取完成"。
- 工单详情：退回原因面板与头部状态标签支持 art_status（美工退回可正常显示）；美工/剪辑分发判定改用 action_type 精确匹配；日志解析失败回退内容转义（修复注入风险）。
- 管理员设置回调带乐观锁；路径检查等死代码清理；日志"系统启动"不再入库。

### Fixed

- 修复工单 2608130944 类"审核通过却无法分发"：剪辑重复提交覆盖已审核状态 + API 失败回滚到过期状态，已恢复数据并修复按钮逻辑。
- 修复恰好双角色（视频后期审核+美工后期审批）用户登录后无法办理工单的问题。
- 修复 `Task.finished` 信号遮蔽 QThread 内置信号；任务超时关闭改为隐藏窗口保留线程（消除 QThread 悬挂崩溃）。
- 修复视频预览：播放错误无提示、duration=-1 滑块异常、图片不随窗口缩放。

## [Unreleased]

### Added

- 新增「美工后期审批」角色与流程：美工分发成品先进入「美工待审批」中转目录，审批通过后才到运营/销售目录。
- 新增合并审批界面：同时拥有「视频后期审核」+「美工后期审批」角色时登录合并为「后期审批」，办理工单可在界面内切换审批类型（仅当工单状态到达对应流程时可用）。
- 新增「美工后期审批」工单状态：美工后期审核中 / 美工后期重新制作，并同步状态筛选、进度条、颜色映射、工单详情步骤链展示。
- 系统设置 → 功能设置 新增「启用美工后期审批功能」开关（art_post_review_enabled），关闭时美工分发回退为直达运营/销售。
- 新增美工专属状态字段 `art_status`（work_orders 表自动迁移），美工链状态与全局 status 解耦，避免与剪辑链状态互相覆盖；状态筛选、进度显示、审批入口均优先读取该字段。
- 新增「路径文件检查」功能（管理员）：工单列表「检查路径」按钮，检查单工单全部关键路径的存在性、文件数、最后修改时间，异常红色高亮。
- 路径文件检查增强：每行支持打开/删除路径（删除带二次确认）；新增「无用内容」列（空文件夹与系统垃圾文件扫描及大小显示）与「推荐删除」列；不存在的路径默认折叠显示；补充「不通过」退回目录检查项；已领取路径自动标注避免误判；加大窗口与按钮尺寸、固定行高列宽。
- 新增「路径权限检查」功能（所有角色）：主界面顶部「路径权限」按钮，按角色检查对应网络路径的读/写权限（写权限通过临时文件实测），管理员检查全部路径；产线级目录按用户所属产线展开检查。

### Changed

- 更新外部 API token（原 token 已过期导致状态同步 401 回滚）。
- 性能优化：路径文件检查改为单次遍历 + 后台线程异步检查，界面不再卡顿；日志表新增 order_id 列与索引（自动迁移并回填历史数据），工单列表批量日志查询由 LIKE 全表扫描改为索引查询（4.8s → 0.8s）。
- 安全加固：数据库密码、管理员密码、API Token 迁移到环境变量/.env 文件读取（参考 .env.example），代码库不再包含明文凭据。
- 数据库改为常驻连接（ping 自动重连，退出时断开），工单列表刷新去重重置查询（单次刷新由 2 次工单查询降为 1 次）；路径权限检查异步化；双击工单详情复用列表日志缓存。
- 统一 API 时间字段映射为单份常量（修复创建接口中美工开始/剪辑开始字段码互换的问题）。
- 状态/时间同步与回滚统一封装（status_sync.py），替换 17 处复制粘贴的回滚逻辑；API 失败时 art_status、成品路径、时间字段均完整回滚（此前仅回滚全局状态）。
- API Token 改为数据库存储（app_system_settings.api_token），管理员设置页「功能设置」新增「外部系统 API 配置」在线更新 Token，保存后立即生效，无需重启；读取优先级：数据库 > 环境变量。
- 修复云上（CI）打包产物无法访问数据库的问题：.env 支持从程序目录/项目根/当前目录多位置加载；Nuitka 打包自动包含 .env；CI 工作流新增从 GitHub Secrets 生成 .env（需在仓库配置 DB1_PASSWORD/DB2_PASSWORD/ADMIN_PASSWORD/API_TOKEN 四个 Secrets）。

### Fixed

- 修复合并审批界面中成品路径为空（None）时视频后期审核页面构建崩溃的问题。
- 修复美工分发/审批状态被剪辑链全局状态覆盖导致进度显示错误、无法进入审批的问题。
- 修复美工审批通过/美工分发异步任务完成回调因对话框已关闭而跳过状态更新、日志与通知的问题（状态更新不再依赖对话框可见性）。
- 修复剪辑进度与详情步骤链仅凭全局状态误判「剪辑已完成」的问题：剪辑链显示改为基于剪辑链自身日志证据（剪辑分发/视频后期审核通过/提交审核），美工链完成不再误显示视频通过。
- 修复文件移动任务清理源目录时对遗留空子目录的误报，移动完成后自动清理空子目录。
- 修复 CI macOS 构建中 hdiutil 创建 DMG 报 Resource busy 的问题：清理扩展属性、等待释放占用并增加重试机制。
- 修复剪辑进度列「后期审核中」误覆盖已通过工单的问题：通过判断优先于进行中判断，工单列表与详情页显示保持一致。
- 修复视频后期审批入口因全局状态被 API 失败回滚而无法打开的问题：剪辑已提交审核且尚未通过（日志证据）时仍可进入审批。
- 修复 SQL 建表脚本外键引用错表名（mcs1_by_takuya_users）导致新库导入失败的问题。
- 修复创建工单时外部系统同步失败无提示的问题（保留本地记录并明确告知用户）。
- 修复摄影上传素材仅本地改状态、外部系统状态不同步的不对称问题。
- 修复美工后期审批预览「下一个/上一个」按钮始终禁用无法切换图片的问题（load_preview 缺失按钮状态同步）。

### Removed

- 删除死表 DDL（roles 重复表、user_departments、work_order_feedback）。
- 删除死方法（init_database/clear_all_data/add_work_order_with_project_info、角色选择旧版死代码、main_window 模块级死函数）。
- 删除 git 跟踪的备份文件 main_window.py.bak/.bak2 与 7 处被注释的 send_dingtalk_markdown 死代码块。
- 统一 IMG_EXTS/VID_EXTS 扩展名常量（.tif/.ts 合并），移除 video_preview 重复定义。
- 更新 project_files.md 与真实代码保持一致。

## [1.16.0] - 2026-04-30

### Added

- 完善 `README.md` 文档结构，新增联调流程、发布流程、最小验收清单。
- 增加界面预览占位区与变更日志入口说明。
- 新增 `CHANGELOG.md` 版本记录模板。

### Changed

- 增强 `CONTRIBUTING.md`，补充分支策略、PR 审核门槛和检查清单。
- 优化 `SECURITY.md`，改为优先使用 Security Advisories 并补充披露策略。

### Fixed

- 移除 `SECURITY.md` 中误导性的占位邮箱描述，降低错误上报风险。

### Removed

- 无
